There is no need to run the second to 8th cells in the python file(It takes about three hours to run). 
As the result of it is already written in the file named 'df_covid.csv' and to use it you should change the correct file path in the 9th cell.
The command "sentence_embeddings = model.encode(sentences)" will take  some time to run, but it will run successfully in the end.
The search tool is in the python file. And whoever wants to use it can change the queries in the 23th cell and it supports custom input.